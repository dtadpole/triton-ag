rsync:
  run_upload: true
  vllm_client: "h8_1"
  rsync_source_prefix: "shared/.trainer"
  rsync_target_prefix: "shared/.trainer"
  rsync_path: "mkdir -p .trainer/{checkpoint_name} && rsync"
  timeout: 10
  retries: 3
  file_list:
    - adapter_config.json
    - adapter_model.safetensors
