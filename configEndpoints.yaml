vllm_clients:
  two:
    api_key_path: "~/.keys/local.api.key"
    host: 192.168.5.202
    port: 8091
    timeout: 60
    retries: 3   # sleep 2^retries seconds between retries

  runpod_1:
    api_key_path: "~/.keys/local.api.key"
    host: gegh8zroa84aao-8091.proxy.runpod.net
    port: 8091
    timeout: 60
    retries: 3   # sleep 2^retries seconds between retries

  runpod_2:
    api_key_path: "~/.keys/local.api.key"
    host: w2ane42ysnjyqq-8091.proxy.runpod.net
    port: 8091
    timeout: 60
    retries: 3   # sleep 2^retries seconds between retries
