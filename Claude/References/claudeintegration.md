user kick off...

take the list of kb files,

ask claude code to generate code / produce queues





add a function for user to use claude code to trigger the kernel bench, generate cuda source code, and integrate w/ the inference eval queue to complete eval & generate benchmark result.



the goal here is to have a claude code base solution, and use it to compare with the custom open soruce model RL fine tuning solution, in kernel bench result