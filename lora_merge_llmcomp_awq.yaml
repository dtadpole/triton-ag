# Base Model, full precision
model_paths:
  base_model: "Qwen/Qwen3-8B"
  lora_adapter: ""
  output_model_path: "finetune_model_output/qwen3_8b_awq/" # path to the output model
  output_model_fullprec_path: "" # path to the output model

data_paths:
  calibration_experience: "alpaca" # "alpaca"  # path to the calibration experience
  eval_experience: "finetune_raw_experiences/"  # path to the evaluation experience

device: cuda:3

quantize_params:
  apply_quantization: true
