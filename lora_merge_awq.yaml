# Base Model, full precision
base_model:
  path: "Qwen/Qwen3-32B" # path to the base model, or the model id in the huggingface model hub

# LoRA Adapther
lora_adapter:
  path: "finetune_model_output/checkpoint-286/"

# Output Model
output_model:
  path: "finetune_model_output/qwen3_32b_test/"

device: cuda:0

calibration_experience:
  path: "finetune_raw_experiences/"
