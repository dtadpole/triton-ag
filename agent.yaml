planner:
  model:
    provider: anthropic
    model: claude-4.0
  run_config:
    max_turns: 30

triton_coder:
  model:
    provider: anthropic
    model: claude-4.0
  run_config:
    max_turns: 60

kernel_bench:
  model:
    provider: anthropic
    model: claude-4.0
  run_config:
    max_turns: 60

kernel_coder:
  model:
    provider: anthropic
    model: claude-4.0
  run_config:
    max_turns: 60

env_setup:
  model:
    provider: anthropic
    model: claude-4.0
  run_config:
    max_turns: 10
