# vLLM Server Configuration
model: "Qwen/Qwen3-8B"  # any HF model vLLM supports
host: "::"
port: 8091
api_key: "dummy"             # vLLM expects "Bearer token-abc123"
allowed_origins: "*"

# Common vLLM knobs (add as needed)
dtype: "auto"
tensor_parallel_size: 4
max_num_seqs: 24768
