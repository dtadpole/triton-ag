registry:
  TC_0.1.0_32B.c:
    short_name: "t32c"
    config_path: "workflow/32B.c.yaml"
    data_dir: "~/.workflow"
  TC_0.1.0_32B.d:
    short_name: "t32d"
    config_path: "workflow/32B.d.yaml"
    data_dir: "~/.workflow"
  TC_0.1.0_32B.e:
    short_name: "t32e"
    config_path: "workflow/32B.e.yaml"
    data_dir: "~/.workflow"
  TC_0.1.0_32B.f:
    short_name: "t32f"
    config_path: "workflow/32B.f.yaml"
    data_dir: "~/.workflow"
  test: # please do not remove, this is used by unit tests
    short_name: "test"
    config_path: "workflow/test.yaml"
    data_dir: "~/.workflow"
  my_tag.a:
    short_name: "ma"
    config_path: "workflow/my_tag.a.yaml"
    data_dir: "~/.workflow"
  # cudacoder_eval_one_turn.a1:
  #   short_name: "cudacoder_eval1"
  #   config_path: "shared/config/workflow/cudacoder_eval_one_turn.a1.yaml"
  #   data_dir: "shared/.workflow"
  # cudacoder_eval_one_turn.a2:
  #   short_name: "cudacoder_eval2"
  #   config_path: "shared/config/workflow/cudacoder_eval_one_turn.a2.yaml"
  #   data_dir: "shared/.workflow"
  # cudacoder_eval_one_turn.sft:
  #   short_name: "cudacoder_eval"
  #   config_path: "shared/config/workflow/cudacoder_eval_one_turn.sft.yaml"
  #   data_dir: "shared/.workflow"
  cudacoder_eval_one_turn.test:
    short_name: "cudacoder_eval2"
    config_path: "shared/config/workflow/cudacoder_eval_one_turn.test.yaml"
    data_dir: "shared/.workflow"

providers:
  default:
    host: "devgpu139.cco2.facebook.com"
    port: 8488
    retries: 5
    timeout: 300

  h8_3:
    host: "devvm8490.cco0.facebook.com"
    port: 8488
    retries: 5
    timeout: 300

# fastapi server
servers:
  default:
    host: "::"
    port: 8488
  one:
    host: 0.0.0.0
    port: 41488
  two:
    host: 0.0.0.0
    port: 42488
  four:
    host: 0.0.0.0
    port: 44488
  dequeue_timeout: 5
  devgpu139.cco2.facebook.com:
    host: "::"
    port: 8488


_refresh_config_task:
  interval: 15

_refresh_workflow_task:
  interval: 15

_save_queue_task:
  interval: 15

_save_adapter_task:
  interval: 15
