sync:
  
  run_upload: true
  vllm_client: "h8_1"
  rsync_source_prefix: "~/.trainer"
  rsync_target_prefix: "devvm3317.eag0.facebook.com:.trainer"
  rsync_path: "mkdir -p .trainer/{checkpoint_name} && rsync"
  timeout: 10
  retries: 3
  file_list:
    - adapter_config.json
    - adapter_model.safetensors
