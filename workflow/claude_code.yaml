# Claude Code Kernel Bench workflow configuration
# Minimal config for queue-based kernel evaluation

queues:
  - name: kbEval.pending

global:
  prefix_tag: "claude_code"
  start_epoch: 0
  start_block: 0
  end_epoch: 1
  end_block: 1
